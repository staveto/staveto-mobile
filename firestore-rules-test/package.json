{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:rules": "firebase emulators:exec --only firestore --project demo-staveto ./run-tests.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.0",
    "jest": "^29.7.0"
  }
}
